# Python dependencies for the CMO import image, installed under the Airflow
# 2.10.5 / py3.12 constraints file (see Dockerfile), which pins exact versions.
# The Python-2-era Google stack (gdata, oauth2client, google-api-python-client,
# httplib2) is intentionally excluded.

# ── DAG runtime ──
boto3
PyYAML
kubernetes
apache-airflow-providers-amazon
apache-airflow-providers-slack

# ── import-scripts/ (modern py3 scripts only) ──
pandas
requests
GitPython
python-json-logger
